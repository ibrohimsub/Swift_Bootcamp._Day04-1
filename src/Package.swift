// swift-tools-version:5.5

import PackageDescription

let package = Package(
    name: "Day04",
    dependencies: [],
    targets: [
        .executableTarget(
            name: "ex01",
            dependencies: []),
        .executableTarget(
            name: "ex02",
            dependencies: []),
	.executableTarget(
            name: "ex03",
            dependencies: []),
    ]
)
